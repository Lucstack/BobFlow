// FILE: src/contexts/MarketplaceContext.js
// DESC: Creates a context to provide marketplace data to any component that needs it.

import { createContext } from 'react';

export const MarketplaceContext = createContext(null);